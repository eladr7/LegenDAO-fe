import { DefaultLayout } from "../components/layouts/DefaultLayout";

function Home(): React.ReactElement {
    return (
        <DefaultLayout>
            <h1 className="mt-4 text-white">LEGEND-DAO#HOME</h1>
        </DefaultLayout>
    );
}

export default Home;
